node_modules/
dist/
out/
*.vsix
.vscode-test/
# Dados locais do Claude Code (sessões, worktrees, memória) — tooling, nunca versionar.
.claude/
admin-cli/keys/
gateway/.env
*.log
.DS_Store
Thumbs.db

# Arquivos gerados ao testar a extensão neste próprio repo.
# Esta extensão é 100% TypeScript/JS — qualquer .py aqui é artefato de teste do FORGE,
# não código-fonte. (Teste o FORGE numa PASTA SEPARADA para evitar isso.)
/*.py
src/**/*.py
data/

# Artefatos Python (ambientes/caches gerados ao testar a extensão aqui)
.venv/
venv/
env/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
